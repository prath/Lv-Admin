apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: admin-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: admin-dashboard-dev.lokaven.com
    http:
      paths:
      - backend:
          serviceName: admin-service
          servicePort: 80